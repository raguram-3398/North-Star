"""Tests for pace/calculator.py — topic_score, timing_ratio, the 80/20
blend, and the sustained-drift threshold check.
"""


def test_placeholder() -> None:
    """Placeholder — real cases to be added once calculator.py is
    implemented, including cold-start (weeks 1-2) and sustained-drift-only
    triggering.
    """
    raise NotImplementedError
